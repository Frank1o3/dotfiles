{
  "version": 10,
  "message": "refactored some of the configs and added a helper tool that install the needed packages and the config",
  "files": [
    "waybar/config.jsonc",
    "waybar/style.css"
  ]
}